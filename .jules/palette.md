## 2025-05-14 - Standardized Button Loading Pattern
**Learning:** Consolidating loading states and accessibility attributes (aria-busy, aria-live) into a core `Button` component prevents UI inconsistencies and reduces boilerplate in forms.
**Action:** Always use the `isLoading` prop in the `Button` component instead of manual spinner rendering to ensure consistent UX and screen reader support.
