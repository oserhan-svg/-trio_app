## 2024-08-04 - Adding ARIA labels to icon-only buttons
**Learning:** Icon-only buttons in AppShell lack accessibility information, rendering them confusing for screen reader users.
**Action:** Always verify icon-only interactive elements and inputs have localized `aria-label` attributes to ensure keyboard and screen reader accessibility.