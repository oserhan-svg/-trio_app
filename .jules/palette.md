## 2025-05-22 - Centralized Loading States
**Learning:** Prioritizing centralizing loading states within the base `Button` component using an `isLoading` prop (with `aria-busy` and `aria-live`) ensures standardized visual feedback and better accessibility than manual string/icon management.
**Action:** Always add or use `isLoading` on `Button` components for async actions.
