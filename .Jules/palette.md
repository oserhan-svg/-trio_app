## 2025-05-22 - [Centralized Loading States]
**Learning:** Consolidating async feedback into a core Button component improves accessibility (aria-busy) and reduces repetitive logic in forms.
**Action:** Use the `isLoading` and `loadingText` props on the `Button` component for all future async operations.
